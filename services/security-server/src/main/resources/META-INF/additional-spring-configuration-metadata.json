{
  "properties": [
    {
      "name": "jwt.secret-key",
      "type": "java.lang.String",
      "description": "It represents the secret key used for JWT (JSON Web Token) signing and verification. This key is used by the application to encode the JWTs and also to validate their integrity when decoding them. "
    },
    {
      "name": "jwt.expiration",
      "type": "java.lang.Long",
      "description": "Json Web Token expiration time"
    }
  ]
}